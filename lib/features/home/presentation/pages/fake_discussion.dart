import 'package:flutter/material.dart';

class FakeDiscussion extends StatelessWidget {
  const FakeDiscussion({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("FakeDiscussion"),
      ),
    );
  }
}