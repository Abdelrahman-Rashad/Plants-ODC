import 'package:flutter/material.dart';

class FakeScan extends StatelessWidget {
  const FakeScan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("FakeScan"),
      ),
    );
  }
}