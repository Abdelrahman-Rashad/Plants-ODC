import 'package:flutter/material.dart';

class FakeExam extends StatelessWidget {
  const FakeExam({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("FakeExam"),
      ),
    );
  }
}